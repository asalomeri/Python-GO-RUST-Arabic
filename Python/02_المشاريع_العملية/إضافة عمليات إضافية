def power(x, y):
    return x ** y

def modulo(x, y):
    if y == 0:
        return "Error: Division by zero"
    return x % y

def square_root(x):
    if x < 0:
        return "Error: Cannot calculate square root of negative number"
    return x ** 0.5
